services:
  api-1: &api
    image: gasparin/rinha-backend-2026@sha256:fad34bc56666611b9f2c162bf7cd0ff7011cd91f663c2d1f9dcddd820c246811
    platform: linux/amd64
    restart: unless-stopped
    networks:
      - rinha-net
    deploy:
      resources:
        limits:
          memory: '160M'
          cpus: "0.45"

  api-2:
    <<: *api

  haproxy:
    image: haproxy:3.0-alpine
    platform: linux/amd64
    restart: unless-stopped
    depends_on:
      - api-1
      - api-2
    ports:
      - "9999:9999"
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    networks:
      - rinha-net
    deploy:
      resources:
        limits:
          memory: '30M'
          cpus: "0.10"

networks:
  rinha-net:
    driver: bridge
