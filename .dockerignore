# .NET build artifacts
**/bin
**/obj

# Local pre-generated artifact (regenerated by the loader stage)
data

# Benchmark project — not part of the production image
bench

# IDE / editor
.vs
.vscode
.idea
**/*.user

# Git
.git
.gitignore
.gitattributes

# Markdown docs — not needed at runtime (but we keep docs-rinha/
# because it contains the dataset that the loader stage reads)
*.md
FDD.json
skills-lock.json

# Logs
*.log
